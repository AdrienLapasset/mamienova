$(document).ready(function () {

  var slider = tns({
    container: '.slider',
    // "center": true,
    autoWidth: true,
    controlsContainer: ".slider-controls",
    mouseDrag: true,
    loop: true,
    // swipeAngle: false,
    nav: false,
    gutter: 90,
    // viewportMax: true,
  });

})

